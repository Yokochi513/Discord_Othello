# Discord アプリケーションの資格情報（OAuth2 トークン交換用）。
# client secret はサーバー側にのみ保持し、リポジトリに含めない（要件定義 §13 / §15）。
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# 以下は任意設定（未指定時のデフォルトは packages/server/src/env.ts を参照）
# PORT=3000
# HOST=0.0.0.0
# STATIC_DIR=
