# Discord オセロゲーム 要件定義書

| 項目 | 内容 |
|---|---|
| 文書名 | Discord オセロゲーム 要件定義書 |
| 版 | 0.1（初版） |
| 最終更新 | 2026-08-21 |
| 作成者 | よこち |
| 位置づけ | 要件定義書。何を作るかを定める。設計・実装の詳細は別途設計書に記す |
| 関連文書 | `要件.md`（本書の元となる構想メモ）／`../sebastian/CLAUDE.md`（連携先の構成） |

---

## 1. 背景・目的

### 1.1 背景

自宅サーバー（ラズベリーパイ）上で、Discord Bot「Sebastian」を運用している。Sebastian は毎朝の挨拶・天気・ニュース・運行情報の投稿、一日一問の資格クイズ、言伝の取り次ぎ、エアコン制御、Gemini による応答などを担っており、既に生活に組み込まれている。

一方でこれらは「Sebastian が一方的に届ける」機能が中心で、家族どうしが Discord 上で能動的に関わり合う要素が薄い。サーバーへの滞在時間と会話のきっかけを増やす余地がある。

### 1.2 目的

Discord 上で遊べる自作オセロを実装し、家族どうしが対戦できる場を作る。あわせて対局の戦績を Sebastian から参照できるようにし、既存 Bot と連携させることで Discord サーバー全体の魅力と使用率の向上を目指す。

### 1.3 成功指標

| 指標 | 測定方法 |
|---|---|
| 月間対局数 | 戦績ファイルの対局レコードを集計 |
| ユニークプレイヤー数（月） | 戦績ファイルの参加者 ID を集計 |
| CPU 対戦と対人対戦の比率 | 戦績ファイルの対戦種別を集計 |
| Sebastian の戦績コマンド実行回数 | Sebastian 側のログを集計 |

いずれも自動投稿ではなく**サーバー側のデータ集計**で測る（§10 の連携方式に自動報告を含めないため）。

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| Activity | Discord のボイスチャンネル等から起動する埋め込み Web アプリ（Embedded App SDK ベース）。本システムの提供形態 |
| オセロサーバー | ラズパイ上で動く Node.js プロセス。静的配信・OAuth 交換・WebSocket 同期・REST API を担う |
| 対局 | 開始から終局までの 1 ゲーム。一意な対局 ID を持つ |
| セッション | 1 人のクライアントとオセロサーバーの WebSocket 接続 |
| ロビー | Activity 起動直後の画面。座席の着席・退席と対局開始を行う |
| 座席 | 黒番・白番の 2 つの枠。着席した者がプレイヤーになる |
| プレイヤー | 座席に着いた参加者。着手できる |
| 観戦者 | Activity を開いているが座席に着いていない参加者。盤面を見るのみ |
| 合法手 | 相手の石を 1 枚以上挟んで反転できるマス |
| パス | 合法手が存在しないために手番を相手に渡すこと |
| 終局 | 両者連続パス、または盤面が石で埋まった状態 |
| 座標 | 列を a〜h、行を 1〜8 で表す（例: `e6`）。左上が `a1` |
| Sebastian | 同じラズパイ上で稼働する既存の Discord Bot。本システムの戦績を参照して報告する |

---

## 3. 想定ユーザー・利用シーン

- **対象**: 単一の私設 Discord サーバー（家族のみ）。メンバーは数名
- **同時対局数**: 通常 1、多くても 2〜3
- **利用デバイス**: PC（デスクトップアプリ／ブラウザ）とスマートフォン
- **利用シーン**: ボイスチャンネルで雑談しながら 1 局打つ／相手がいない時に CPU と打つ
- **前提**: 利用者はオセロのルールを概ね知っている。ただし合法手の見落としは頻繁に起きるため、打てる場所は画面上で明示する

---

## 4. スコープ

### 4.1 対象（MVP）

- Discord Activity としての提供
- ロビー（座席制）と観戦
- 8×8 標準ルールの対人対局
- CPU 対局（強度 3 段階）
- 合法手のハイライト表示
- パスの自動処理、投了、対局の中断
- 終局判定と結果表示
- 対局状態・戦績のファイル永続化
- 切断からの再接続
- Sebastian のコマンドから戦績・ランキングを参照できる REST API

### 4.2 対象（フェーズ2以降）

- 棋譜のリプレイ再生
- 対局の振り返り（CPU による最善手の提示）
- 実績・称号
- 対局中のリアクション／スタンプ

### 4.3 対象外

| 項目 | 理由 |
|---|---|
| 対局結果の Discord チャンネルへの自動投稿 | §10 の連携方式を「コマンド時のみ取得」としたため、自動で流す経路を持たない |
| 待った・手戻し | 対局の単純さを保つため |
| 手番の持ち時間 | §7.6 の判断による |
| レーティング（Elo 等） | 母数が数名では機能しない |
| 盤面サイズの可変・変則ルール | 標準ルールに固定する |
| 3 人以上・チーム戦 | オセロは 2 人用 |
| サーバー横断のマッチング | 単一の私設サーバー専用 |
| 多言語対応 | 日本語固定 |
| 冗長化・CI/CD・監視基盤 | 個人運用の規模に対して過剰 |

### 4.4 判断保留

§20 の未決事項に集約する。

---

## 5. システム構成

### 5.1 全体構成

```
  Discord クライアント（PC / スマホ / ブラウザ）
        │
        │  iframe で Activity を読み込む
        ▼
  ┌──────────────────────────────────┐
  │  Cloudflare Tunnel               │  既存の独自ドメインのサブドメイン
  │  （ポート開放なし・証明書自動）  │
  └──────────────────────────────────┘
        │
        ▼
  ┌──────────────────────────────────┐
  │  ラズベリーパイ                  │
  │                                  │
  │  ┌────────────────────────────┐  │
  │  │ オセロサーバー (Node.js)   │  │
  │  │  ├ 静的ファイル配信        │  │
  │  │  ├ OAuth2 トークン交換     │  │
  │  │  ├ WebSocket 対局同期      │  │
  │  │  ├ ゲームエンジン（権威）  │  │
  │  │  └ REST API（戦績照会）    │  │
  │  └────────────────────────────┘  │
  │            ▲                     │
  │            │ localhost / HTTP    │
  │  ┌────────────────────────────┐  │
  │  │ Sebastian (Python)         │  │
  │  │  └ /othello_* コマンド     │  │
  │  └────────────────────────────┘  │
  └──────────────────────────────────┘
```

### 5.2 技術スタック

| 層 | 採用 | 選定理由 |
|---|---|---|
| 提供形態 | Discord Activities（Embedded App SDK） | マウス／タップで盤面を直接操作でき、ゲームとしての魅力が最も高い |
| フロントエンド | React + Vite + TypeScript | Discord 公式サンプルが React であり、詰まったときの参照例が最も多い。4 画面と WebSocket 由来の状態を扱うため素の DOM 操作では管理が苦しい |
| 盤面描画 | DOM（CSS Grid で 8×8） | 64 マス程度ならクリック判定を自前で書く必要がなく、石の反転も CSS transition で済む。Canvas は過剰 |
| バックエンド | Node.js + TypeScript | フロントと同一言語。盤面ロジックを 1 本書いて両方で共有でき、型でずれを防げる |
| リアルタイム同期 | WebSocket | 対局状態の即時反映のため |
| 永続化 | JSON ファイル（原子的書き込み） | 家族数名の規模に対し DB は過剰。Sebastian の `quiz_store.py` が確立した「一時ファイル→`os.replace`」方式を TypeScript で再現する |
| 公開 | Cloudflare Tunnel | ルータのポート開放が不要で自宅 IP を露出しない。証明書は自動。WebSocket も通る |
| プロセス管理 | systemd | Sebastian の `sebastian.service` に倣う |

**Bot は持たない。** Discord のテキストチャンネルへの出力は Sebastian が担うため、オセロ側に discord.js の Bot は不要。Discord アプリケーションは Sebastian（Bot）と本 Activity の 2 つになる。

### 5.3 レイヤー構成

盤面ロジックは Discord にも WebSocket にも依存させず、独立したモジュールとして切り出す。ユニットテストの対象はここに集中させる。

```
packages/
  core/       盤面・合法手・反転・終局判定・CPU 思考（Discord 非依存・テスト対象）
  server/     HTTP / WebSocket / OAuth / REST API / 永続化
  client/     React による画面（core を型と判定の参照に使う）
```

### 5.4 権威モデル

**サーバー権威**とする。クライアントは iframe 内の Web アプリであり改変可能なため、合法手判定・反転・終局判定は必ずサーバー側で実行する。クライアント側の同一ロジックは、打てるマスのハイライトなど**表示の即応性のためだけ**に使い、着手の正当性はサーバーの判定を唯一の正とする。

---

## 6. ゲームルール仕様

| 項目 | 仕様 |
|---|---|
| 盤面 | 8×8。列 a〜h、行 1〜8。左上が `a1` |
| 初期配置 | `d4`=白、`e4`=黒、`d5`=黒、`e5`=白 |
| 先手 | 黒。どちらが黒になるかはロビーでの着席により決まる |
| 合法手 | 空マスに石を置いたとき、8 方向のいずれかで相手の石を 1 枚以上挟めるマス |
| 反転 | 挟んだ方向すべての相手の石を反転する |
| パス | 手番側に合法手が 1 つも無いとき、**自動的に**手番を相手へ渡す。画面にパスが発生した旨を表示する |
| 終局 | ① 両者が連続でパス ② 盤面が石で埋まる ③ 一方の石が 0 枚になる |
| 勝敗 | 終局時の石数が多い方の勝ち。同数は引き分け |
| 投了 | いつでも可能。投了した側の負けとして記録する。石数は投了時点の値を記録する |
| 中断 | 両者の合意なしに一方が中断した場合、その対局は**無効試合**として扱い、戦績に記録しない |
| 準拠ルール | 一般的なリバーシのルールに準拠する。公式オセロ規則の細則（時間規定・記録用紙等）は対象外 |

---

## 7. 画面・UI 仕様

### 7.1 画面一覧

| # | 画面 | 役割 |
|---|---|---|
| 1 | ロビー | 座席の着席・退席、CPU の着席、対局開始 |
| 2 | 対局 | 盤面表示と着手 |
| 3 | 終局 | 結果表示、再戦、ロビーへ戻る |
| 4 | 戦績 | 自分と参加者の通算成績（Activity 内） |

### 7.2 ロビー画面

```
── オセロ ──

⚫ 黒番  よこち      [退席]
⚪ 白番  （空席）    [着席] [CPUを座らせる]

観戦中: はりま, お嬢様

        [ 対局開始 ]
         ↑両席が埋まるまで無効
```

- 参加者の表示名とアバターは Discord から取得する
- 座席に着いていない参加者は自動的に観戦者になる
- CPU を座らせる場合は強度（やさしい／ふつう／つよい）を選ぶ
- 対局開始は、両席が埋まっているときのみ押せる
- 進行中の対局がある状態で Activity を開いた場合は、ロビーを飛ばして対局画面へ復帰する

### 7.3 対局画面

- 盤面は CSS Grid の 8×8。マスをクリック／タップして着手する
- **合法手を明示する**（打てるマスに視覚的な印を出す）。手番でない側には印を出さない
- 常に表示する情報: 黒白の石数、現在の手番、手数、両プレイヤーの表示名と接続状態
- 石の反転はアニメーションさせる
- 直前に打たれた手を盤面上で示す
- 操作ボタン: 投了、中断
- 観戦者には盤面と情報のみを表示し、マスのクリックは受け付けない

### 7.4 終局画面

- 勝敗（勝ち／負け／引き分け）と最終石数
- 最終盤面
- 「もう一局」（同じ座席で再戦）と「ロビーへ戻る」

### 7.5 表示言語・文体

全文日本語。文体は Sebastian の執事口調に寄せず、ゲーム画面として簡潔な表記とする（Sebastian が報告する §10 の出力のみ執事口調）。

### 7.6 手番の時間制限

**設けない。** 家族が雑談しながら打つ利用シーンを想定しており、不可抗力の切断で敗北が決まる事態を避ける。時間制限がない代わりに、長時間動きのない対局はサーバー側で自動破棄する（§11.4）。

---

## 8. 機能要件

優先度: **MUST** = MVP 必須、**SHOULD** = MVP に含めたい、**COULD** = フェーズ2以降

| ID | 機能 | 概要 | 優先度 |
|---|---|---|---|
| F-01 | Activity 起動 | ボイスチャンネルから起動し、Discord OAuth2 で利用者を識別する | MUST |
| F-02 | ロビー表示 | 参加者一覧・座席状況・観戦者一覧を全員に同期表示する | MUST |
| F-03 | 着席／退席 | 黒席・白席への着席と退席。既に埋まった席には着席できない | MUST |
| F-04 | CPU 着席 | 空席に指定強度の CPU を座らせる | MUST |
| F-05 | 対局開始 | 両席が埋まった状態で対局を生成し、全員を対局画面へ遷移させる | MUST |
| F-06 | 盤面同期 | 着手のたびに全参加者（観戦者含む）へ最新盤面を配信する | MUST |
| F-07 | 着手 | 手番のプレイヤーの合法手のみを受理し、反転を確定させる | MUST |
| F-08 | 合法手表示 | 手番側に打てるマスを明示する | MUST |
| F-09 | パス処理 | 合法手が無い場合に自動でパスし、その旨を通知する | MUST |
| F-10 | 終局処理 | 終局条件を判定し、勝敗を確定して戦績へ記録する | MUST |
| F-11 | 投了 | プレイヤーが投了でき、相手の勝ちとして記録する | MUST |
| F-12 | 中断 | 対局を破棄する。戦績には記録しない | MUST |
| F-13 | 観戦 | 座席に着いていない参加者が盤面を閲覧できる | MUST |
| F-14 | 再接続 | 切断後に再度開いた際、進行中の対局へ復帰する | MUST |
| F-15 | 対局復元 | サーバー再起動後、保存済みの対局を復元する | MUST |
| F-16 | 戦績記録 | 終局した対局を永続化する | MUST |
| F-17 | 戦績 API | Sebastian が参照する REST API を提供する | MUST |
| F-18 | 戦績表示 | Activity 内で自分と参加者の通算成績を表示する | SHOULD |
| F-19 | 再戦 | 終局後、同じ座席で次の対局を始める | SHOULD |
| F-20 | 棋譜保存 | 対局の全着手を記録する | SHOULD |
| F-21 | リプレイ | 保存した棋譜を再生する | COULD |

---

## 9. CPU 対戦仕様

### 9.1 強度

| 表示名 | アルゴリズム | 想定される強さ |
|---|---|---|
| やさしい | 角が取れるなら取る、そうでなければ最も多く返る手（貪欲法） | オセロを覚えたばかりでも勝てる |
| ふつう | Minimax + α-β 枝刈り、探索深さ 4〜6 | 定石を知らない相手には勝つ |
| つよい | 探索深さ 8 以上、位置評価・着手可能数・確定石を含む評価関数、終盤は完全読み | 慣れた人でも苦戦する |

### 9.2 制約

- 思考はサーバー側（Node.js）で実行する
- **思考時間に上限を設ける**。上限に達した時点で、その時点の最善手を返す（反復深化）
- ラズパイの CPU 性能に対して各強度の探索深さが妥当かは、実機での実測により決定する（§20）
- 思考中はその旨を画面に表示し、対局が固まったように見えないようにする
- 思考が Node.js のイベントループを占有して WebSocket の応答を止めないよう、別スレッド（worker_threads）または分割実行とする

---

## 10. Sebastian 連携仕様

### 10.1 方式

**Sebastian からのコマンド時 pull のみ。** 自動報告・push は行わない。

- Sebastian に `/othello_stats`（個人戦績）と `/othello_rank`（ランキング）に相当するスラッシュコマンドを追加する
- 実行時、Sebastian が **localhost** 経由でオセロサーバーの REST API を GET し、取得した内容を執事口調に整形して応答する
- Sebastian 側は新たな待ち受けポートを持たない。既存の `aiohttp` 依存をクライアントとして使うのみ

### 10.2 API（概略）

| メソッド | パス | 内容 |
|---|---|---|
| GET | `/api/stats/{user_id}` | 指定ユーザーの通算成績（勝／敗／引分／勝率、対人・CPU 別） |
| GET | `/api/rank` | ランキング（集計期間・並び順はクエリで指定） |
| GET | `/api/health` | 稼働確認 |

- **待ち受けは localhost に限定する。** Cloudflare Tunnel で外部公開するのは Activity 用のパスのみとし、`/api/` は外部へ露出させない
- ユーザーの識別子は Discord User ID を共通キーとする
- オセロサーバーが停止していた場合、Sebastian は例外を投げず日本語のフォールバック文（例:「ただいまオセロの記録に手が届きませぬ」）を返す。これは Sebastian の既存規約（外部取得は自身で例外を捕捉し、日本語のフォールバックを返す）に従う

### 10.3 Sebastian 側の改修範囲

- `bot/services/` に本 API のフェッチャを 1 つ追加する（既存の `weather.py` 等と同じ形）
- `bot/cogs/sebastian.py` にスラッシュコマンドを追加する
- `bot/config.py` に API のベース URL を追加する
- Sebastian の既存機能（朝の投稿・エアコン制御・Gemini 応答）には影響を与えない

---

## 11. データ設計・永続化

### 11.1 保存方式

JSON ファイルへの保存とし、DB は使わない。書き込みは**一時ファイルへ書いてから `rename` で置き換える**原子的操作とし、途中で落ちても既存ファイルが壊れないようにする（Sebastian の `bot/quiz_store.py` と同方式）。読み込みに失敗した場合は例外を投げず、ログに残して空の状態から続行する。

### 11.2 保存対象

| ファイル | 内容 | 更新契機 |
|---|---|---|
| 進行中対局 | 対局 ID、盤面、手番、プレイヤー、手数、棋譜、最終更新時刻 | 1 手ごと |
| 戦績 | 終局した対局の記録（対局 ID、両者、対戦種別、勝者、石数、日時） | 終局時 |

### 11.3 切断と再接続

- WebSocket が切断されても、**一定時間は座席を保持する**（猶予時間は §20）
- 猶予内に同じユーザーが再接続した場合、進行中の局面へ復帰する
- 猶予を過ぎた場合の扱いは §14 に定める
- サーバー再起動時は、保存済みの進行中対局を読み込んで復元する。クライアントは再接続すれば局面から続けられる

### 11.4 保持期間と掃除

- 最終更新から一定時間を過ぎた進行中対局は、サーバー側で自動破棄する（無効試合扱い、戦績には記録しない）
- 戦績は保持期間を設けて古いレコードを削除し、ファイルの肥大を防ぐ
- 保存先はリポジトリ直下に置き、**git 管理外**とする（Sebastian の運用に倣う）

### 11.5 個人情報

保持するのは Discord User ID と表示名のみ。メールアドレス等は取得も保存もしない。

---

## 12. 非機能要件

| 分類 | 要件 |
|---|---|
| 応答性 | 着手から全参加者の盤面が更新されるまで 1 秒以内（CPU の思考時間を除く） |
| CPU 思考時間 | 各強度で上限を定め、上限内に必ず着手する |
| 同時対局数 | 同時 3 対局を処理できること |
| 初期表示 | Activity 起動から操作可能になるまで 5 秒以内 |
| 可用性 | 常時稼働。計画停止（更新デプロイ）中の対局は永続化により復元されること |
| リソース | ラズパイ上で Sebastian と共存できるメモリ使用量に収めること |
| 保守性 | 盤面ロジックを Discord 非依存モジュールに分離し、ユニットテストを備えること |
| 移植性 | ラズパイ以外の Linux でも同じ手順で動作すること |
| ログ | 対局の開始・終了・エラー・接続断を記録すること。ローテーションは journald に委ねる |
| 国際化 | 日本語固定。多言語化は行わない |

---

## 13. 制約条件

| 分類 | 制約 |
|---|---|
| ハードウェア | ラズベリーパイ 1 台。Sebastian と同居する |
| ネットワーク | 自宅回線。Cloudflare Tunnel により外向き接続のみを使用し、ポート開放は行わない |
| ドメイン | 所有済みの独自ドメインのサブドメインを割り当てる。ネームサーバーを Cloudflare に向ける必要があるため、既存の DNS レコードの移行を確認すること |
| Discord | Activity は Developer Portal での URL Mapping 設定が必要。外部通信は Discord のプロキシ（`/.proxy/`）経由に限定される |
| Discord | Activity の iframe には CSP が適用される。外部 CDN への直参照は避け、資産は自前で配信する |
| 認証 | OAuth2 の client secret はサーバー側に保持し、クライアントへ渡さない |
| 開発体制 | 開発者は 1 名。実装量が直接スケジュールに効く |
| 予算 | 追加の月額費用は発生させない（ドメインは所有済み、Cloudflare Tunnel は無料枠） |

---

## 14. 異常系・エラー処理

| # | 事象 | 扱い |
|---|---|---|
| E-01 | 手番でない者が着手した | サーバーが拒否し、クライアントへエラーを返す。盤面は変えない |
| E-02 | 非合法手が送られた | サーバーが拒否する。改変クライアントを想定し、必ずサーバー側で検証する |
| E-03 | 観戦者が着手を送った | 拒否する |
| E-04 | 既に終局した対局への着手 | 拒否し、終局画面へ誘導する |
| E-05 | 存在しない対局 ID の指定 | ロビーへ戻す |
| E-06 | プレイヤーが切断した | 猶予時間は席を保持し、画面に接続状態を表示する。猶予超過時の扱いは §20 で決める |
| E-07 | 両者が切断した | 対局を保存したまま待機し、§11.4 の掃除対象になったら破棄する |
| E-08 | サーバーが再起動した | 保存済み対局を復元する。クライアントは自動で再接続を試みる |
| E-09 | 対局中に一方がサーバーを離脱した | 対局を無効試合として終了する |
| E-10 | 永続化ファイルが壊れていた | 例外を投げず、ログへ記録して空の状態から続行する |
| E-11 | OAuth のトークン交換に失敗した | エラー画面を出し、再試行を促す |
| E-12 | CPU の思考が上限時間を超えた | その時点の最善手を打つ |
| E-13 | Cloudflare Tunnel が切断した | systemd により再接続する。復旧後にクライアントが再接続すれば対局は継続する |
| E-14 | Sebastian からの API 参照に失敗した | Sebastian 側が日本語のフォールバック文を返し、例外を Discord に露出させない |
| E-15 | 想定外の例外 | ログへ記録し、対局は破棄せず現在の局面を保持する。利用者には汎用のエラー表示を出す |

---

## 15. セキュリティ

- OAuth2 の client secret、Discord アプリケーションの資格情報は環境変数で管理し、リポジトリに含めない（`.gitignore`）
- 着手の正当性は必ずサーバー側で検証する。クライアントからの入力は一切信用しない
- REST API（`/api/`）は localhost からのみ受け付け、Cloudflare Tunnel の公開対象に含めない
- 対局への参加可否は、Discord から得たユーザー ID と Activity のインスタンス ID で判定する
- WebSocket のメッセージは対局 ID とユーザー ID で認可し、他の対局への干渉を防ぐ
- 依存パッケージの脆弱性は定期的に確認する
- ラズパイのポート開放は行わない（Cloudflare Tunnel により外向き接続のみ）

---

## 16. デプロイ・運用

### 16.1 環境

| 環境 | 用途 |
|---|---|
| 開発 | 開発機。Vite の開発サーバーと、Discord の開発用 URL Mapping を使う |
| 本番 | ラズパイ。Cloudflare Tunnel 経由で公開 |

### 16.2 構成

- フロントを Vite でビルドし、成果物をオセロサーバーが静的配信する
- プロセス管理は systemd。`sebastian.service` に倣い、`Restart=always` とし、標準出力がバッファリングで失われないようにする
- Cloudflare Tunnel は `cloudflared` を systemd サービスとして常駐させる
- 設定（ポート、ドメイン、OAuth 資格情報、保持期間、CPU 思考時間上限）は環境変数で外部化する

### 16.3 デプロイ手順

1. リポジトリを取得
2. 依存をインストールし、フロントをビルド
3. サービスを再起動

進行中の対局は永続化により再起動をまたいで復元されるため、無停止更新は要件としない。

### 16.4 Discord 側の設定

- Developer Portal で Activity を有効化し、URL Mapping を設定する
- 対象は自分の Discord サーバーのみ。一般公開・審査申請は行わない

---

## 17. テスト方針

| 対象 | 方法 |
|---|---|
| 盤面ロジック | ユニットテスト。合法手判定・反転・パス・終局判定を、既知の局面を用いて網羅的に検証する |
| CPU 思考 | 既知の局面で妥当な手を選ぶこと、思考時間上限を守ることを検証する |
| 永続化 | 保存・読み込み・破損ファイルからの復旧を検証する |
| 同期 | 2 クライアントを接続し、着手が双方に反映されることを確認する |
| 再接続 | 切断・再接続・サーバー再起動をまたいで局面が保たれることを確認する |
| 異常系 | §14 の各項目を手動で再現する |
| 実機 | ラズパイ上で CPU 各強度の思考時間を実測する |

**受け入れ基準**: 2 人が対人で 1 局を終局まで打ち切れ、CPU 3 段階すべてと 1 局ずつ終局まで打て、途中でリロードしても局面が失われないこと。

---

## 18. マイルストーン

| # | フェーズ | 完了条件 |
|---|---|---|
| M1 | ゲームエンジン | 盤面ロジックのユニットテストが通る |
| M2 | Activity の疎通 | ラズパイ上の Activity が Discord 内で起動し、OAuth で利用者を識別できる |
| M3 | 対人対局 | ロビーから対局・終局まで 2 人で通しで打てる |
| M4 | CPU 対局 | 3 段階の CPU と打てる |
| M5 | 永続化・再接続 | 切断とサーバー再起動をまたいで対局が続く |
| M6 | Sebastian 連携 | Sebastian のコマンドから戦績とランキングを引ける |

M1〜M5 が MVP。M2 は技術的な不確実性が最も高いため、可能な限り早い段階で疎通だけ先に確認する。

---

## 19. リスクと対策

| # | リスク | 影響 | 対策 |
|---|---|---|---|
| R-01 | Activity の URL Mapping や CSP に阻まれて動かない | 致命的（着手不能） | M2 で最小構成の疎通を最優先で確認する。ここが通らなければ Bot 版へ退避する判断をする |
| R-02 | ラズパイの CPU 性能が「つよい」の探索に耐えない | CPU の強度が名前倒れになる | 反復深化＋思考時間上限で、性能に応じて深さが自動的に決まる設計にする |
| R-03 | CPU 思考がイベントループを塞ぎ、対局が固まる | 体験の悪化 | worker_threads か分割実行にする |
| R-04 | 自宅回線・停電による停止 | 対局の中断 | 永続化により復旧後に再開できるようにする |
| R-05 | SD カードの故障 | 戦績の消失 | 戦績ファイルを定期的にバックアップする |
| R-06 | Sebastian と同居することによるメモリ不足 | 双方の不安定化 | メモリ使用量を計測し、必要なら CPU の探索テーブルを縮める |
| R-07 | Discord の Activities 仕様変更 | 動作しなくなる | 仕様変更の可能性を前提とし、盤面ロジックを Discord 非依存に保つ |
| R-08 | 実装量が個人開発の許容量を超える | 未完成のまま停滞 | M3（対人対局）で一度遊べる状態にし、CPU 強化は後追いにできる構成にする |
| R-09 | 作ったが遊ばれない | 目的未達 | M3 到達時点で実際に家族と打ち、続きを作るか判断する |

---

## 20. 未決事項

| # | 項目 | 選択肢・検討事項 | 決定期限 |
|---|---|---|---|
| O-01 | 再接続の猶予時間 | 3 分／5 分／10 分。短いと不便、長いと席が塞がる | M5 まで |
| O-02 | 猶予超過時の扱い | 無効試合として破棄／切断側の負け／席だけ空けて対局は保持 | M5 まで |
| O-03 | 進行中対局の自動破棄までの時間 | 6 時間／24 時間／72 時間 | M5 まで |
| O-04 | 戦績の保持期間 | 無期限／13 か月 | M6 まで |
| O-05 | ランキングの集計期間と並び順 | 今月／通算、勝率／勝数。勝率なら最低対局数の下限が必要 | M6 まで |
| O-06 | CPU 各強度の探索深さと思考時間上限 | ラズパイでの実測により決定する | M4 まで |
| O-07 | 割り当てるサブドメイン名 | — | M2 まで |
| O-08 | Sebastian に追加するコマンド名 | `/othello_stats` `/othello_rank` 等。既存コマンドとの命名の一貫性を確認する | M6 まで |
| O-09 | 観戦者数の上限 | 家族規模では実質不要だが、上限の有無を決める | M3 まで |
| O-10 | 再戦時に黒白を入れ替えるか | 入れ替える／固定 | M3 まで |
| O-11 | 引き分けの戦績上の扱い | 勝率の分母に含めるか | M6 まで |

---

## 21. 参考資料

- Discord Developer Documentation — Embedded App SDK / Activities
- Discord Developer Documentation — OAuth2
- Cloudflare Tunnel ドキュメント
- Sebastian リポジトリ（`../sebastian`）— `CLAUDE.md`、`bot/quiz_store.py`（永続化方式）、`sebastian.service`（systemd の書き方）
